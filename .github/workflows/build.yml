name: Build Reusable
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string #數字、布林、字串
jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - run: echo "Building version ${{ inputs.version }}"
